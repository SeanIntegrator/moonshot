/**
 * Feature flag key registry — contract lives in `@moonshot/types`;
 * re-exported here so apps can import domain runtime from one package.
 */

export {
  FeatureFlagKeys,
  type FeatureFlagKey,
} from '@moonshot/types';
